--- a/drivers/net/wwan/rpmsg_wwan_ctrl.c
+++ b/drivers/net/wwan/rpmsg_wwan_ctrl.c
@@ -148,6 +148,12 @@
 static const struct rpmsg_device_id rpmsg_wwan_ctrl_id_table[] = {
 	/* RPMSG channels for Qualcomm SoCs with integrated modem */
 	{ .name = "DATA5_CNTL", .driver_data = WWAN_PORT_QMI },
+	/*
+	 * Samsung msm8917 (gta2slte): прошивка не создаёт QMI-канал
+	 * сама — он рождается после QMI DPM Open Port (см. службу
+	 * t385-modem-dpm в пакете устройства) под именем DATA40_CNTL.
+	 */
+	{ .name = "DATA40_CNTL", .driver_data = WWAN_PORT_QMI },
 	{ .name = "DATA4", .driver_data = WWAN_PORT_AT },
 	{ .name = "DATA1", .driver_data = WWAN_PORT_AT },
 	{},
